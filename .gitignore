# macOS
.DS_Store

# Editor scratch
*.swp
*~

# Local tooling, not part of the project
.storecode/

# Nothing generated lives in the repo: config goes to ~/.config/dotfiles,
# state goes to ~/.local/state/dotfiles. If something shows up here that
# needs ignoring, that is usually a sign it was written to the wrong place.
