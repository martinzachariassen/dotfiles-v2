# jq is also in dev-cli, on purpose: modules cannot depend on each other and
# claude-code sorts first. `brew bundle` is idempotent.

cask "claude-code@latest"
brew "jq" # statusline.sh parses its stdin with it
